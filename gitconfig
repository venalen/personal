[user]
	name = Vicky Enalen
	email = enalenv@gmail.com

[alias]
	co = checkout
	st = status
	br = branch
[core]
	editor = nvim

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

